interface:
  display_name: "SP MCP"
  short_description: "Repo-specific patterns and workflows for SP-MCP"
  default_prompt: "Use the SP-MCP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true